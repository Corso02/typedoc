const SECOND_IN_MILLISECONDS = 1000;

const trueVal = true;

const falseVal = false;

const bigNumber = 9007199254740991n;

const zero = 0;

const negativeNum = -10;

const float = 8.85;

const bigNegativeNum = -9007199254740991n;

const str = "hello world";

const escaped_str_end = "Testing with escaped at the end\n";

const escaped_str_middle = "Testing with \nescaped in the middle";

const escaped_str_start = "\nTesting with escaped at the start";

const obj = { key: "string", value: "this is awesome" };

const arr = [1, 2, 3, 4, 5];
